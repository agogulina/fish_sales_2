# ядро 
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
openpyxl>=3.1

# Слой 1 (поиск)
sentence-transformers>=2.2
faiss-cpu>=1.7
rapidfuzz>=3.0
pymorphy3>=1.2
scikit-posthocs>=0.7
# transformers, accelerate, bitsandbytes  # только для LLM-метода 

# Слой 2 (причинность)
statsmodels>=0.14
# econml>=0.14   # CATE causal forest 
# dowhy>=0.11    # refutation 
# mapie>=0.8     # конформные интервалы 

# Слой 3 (оптимизация)
pulp>=2.7
